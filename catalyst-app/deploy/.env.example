# Use latest after the main branch's Container image workflow has succeeded.
# For a reproducible deployment, use the sha-<commit> tag from GitHub Actions.
CATALYST_IMAGE=ghcr.io/ryank311/catalyst-coach:latest
